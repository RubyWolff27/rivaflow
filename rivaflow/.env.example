# RivaFlow Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to git (it's in .gitignore)

# ============================================================================
# REQUIRED VARIABLES
# ============================================================================

# Secret key for JWT tokens and session encryption
# Generate with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
# MUST be at least 32 characters long
SECRET_KEY=your-secret-key-here-minimum-32-characters-long

# Database connection string
# SQLite (development): sqlite:///./rivaflow.db
# PostgreSQL (production): postgresql://user:password@host:port/database
DATABASE_URL=sqlite:///./rivaflow.db

# ============================================================================
# ENVIRONMENT CONFIGURATION
# ============================================================================

# Environment: development, staging, or production
# Affects logging, security headers, error messages
ENV=development

# ============================================================================
# CORS & SECURITY
# ============================================================================

# Allowed origins for CORS (comma-separated)
# Development: Leave empty to use localhost defaults
# Production: Set to your frontend domains
# Example: https://rivaflow.app,https://www.rivaflow.app,https://api.rivaflow.app
ALLOWED_ORIGINS=

# ============================================================================
# OPTIONAL - EXTERNAL SERVICES
# ============================================================================

# Redis connection string (optional, for caching)
# Format: redis://host:port/db or redis://user:password@host:port/db
# If not set, caching will be disabled (fallback mode)
REDIS_URL=

# SendGrid API key (optional, for email notifications)
# Get yours at: https://app.sendgrid.com/settings/api_keys
# If not set, email notifications will be disabled
SENDGRID_API_KEY=

# Email from address (used with SendGrid)
# Example: noreply@rivaflow.app
EMAIL_FROM=noreply@rivaflow.app

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================

# Render.com:
# - Set these in Dashboard → Environment → Environment Variables
# - SECRET_KEY: Click "Generate Value"
# - DATABASE_URL: Auto-populated when you connect a database
# - ALLOWED_ORIGINS: Add your custom domain(s)

# Local Development:
# 1. Copy this file: cp .env.example .env
# 2. Generate SECRET_KEY: python -c 'import secrets; print(secrets.token_urlsafe(32))'
# 3. Keep DATABASE_URL as SQLite for local dev
# 4. Leave ALLOWED_ORIGINS empty (uses localhost defaults)

# Production Checklist:
# ✓ SECRET_KEY is strong (32+ chars, random)
# ✓ DATABASE_URL points to PostgreSQL
# ✓ ENV=production
# ✓ ALLOWED_ORIGINS set to your domains
# ✓ REDIS_URL set for caching (recommended)
# ✓ SENDGRID_API_KEY set for emails (recommended)
