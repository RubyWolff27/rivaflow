# RivaFlow Production Dependencies - All versions pinned for reproducibility
# Last updated: 2026-02-04 (Production deployment preparation)
# Run `pip-audit` periodically to check for vulnerabilities

# Core framework
fastapi==0.135.4  # Bumped from 0.128.4 to allow starlette>=1.0.1 (CVE PYSEC-2026-161). Range now starlette>=0.46.0.
starlette==1.3.1  # CVE-2026-48817/48818, PYSEC-2026-248/249 (also PYSEC-2026-161)
uvicorn[standard]==0.40.0
gunicorn==23.0.0
typer==0.21.1
rich==13.9.4
pydantic==2.12.5
pydantic-settings==2.14.2  # GHSA-4xgf-cpjx-pc3j

# Authentication & security
PyJWT[crypto]==2.13.0  # bumped from 2.12.0 — patches PYSEC-2026-175..179
passlib[bcrypt]==1.7.4
bcrypt==4.3.0
cryptography==48.0.1  # GHSA-537c-gmf6-5ccf (also CVE-2026-39892)

# HTTP & API
python-multipart==0.0.31  # CVE-2026-53538/53539/53540
httpx==0.28.1

# Database
psycopg2==2.9.11
pgvector==0.4.2

# Configuration & utilities
python-dotenv==1.2.2  # CVE-2026-28684 fix
email-validator==2.3.0

# Rate limiting & caching
slowapi==0.1.9
redis==5.3.1

# Background job scheduling
apscheduler>=3.10,<4

# Monitoring & error tracking
sentry-sdk[fastapi]==2.52.0

# Email
sendgrid==6.12.5

# File type detection (Python 3.13+ compatibility - replaces deprecated imghdr)
filetype==1.2.0

# Cloud storage (S3 / Cloudflare R2)
boto3==1.36.14

# Structured logging
python-json-logger==3.3.0

# AI/ML dependencies (Grapple AI Coach)
# TEMPORARILY DISABLED: Large CUDA dependencies cause deployment failures
# Re-enable when migrating AI features to separate microservice or using CPU-only torch
# groq==0.14.0
# together==1.5.35
# sentence-transformers==3.3.1
# tiktoken==0.8.0
jinja2==3.1.6
