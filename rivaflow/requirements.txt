# RivaFlow Production Dependencies - All versions pinned for reproducibility
# Last updated: 2026-02-04 (Production deployment preparation)
# Run `pip-audit` periodically to check for vulnerabilities

# Core framework
fastapi==0.136.1
uvicorn[standard]==0.47.0
gunicorn==23.0.0
typer==0.25.1
rich==13.9.4
pydantic==2.13.4
pydantic-settings==2.14.1

# Authentication & security
PyJWT[crypto]==2.12.1  # CVE-2026-32597 fix
passlib[bcrypt]==1.7.4
bcrypt==4.3.0
cryptography==46.0.7  # CVE-2026-39892 fix (was 46.0.6 for CVE-2026-34073, was 46.0.5 for CVE-2026-26007)

# HTTP & API
python-multipart==0.0.29  # CVE-2026-40347 + CVE-2026-42561 fix (was 0.0.22 for CVE-2026-24486)
httpx==0.28.1

# Database
psycopg2==2.9.12
pgvector==0.4.2

# Configuration & utilities
python-dotenv==1.2.2  # CVE-2026-28684 fix
email-validator==2.3.0

# Rate limiting & caching
slowapi==0.1.9
redis==5.3.1

# Background job scheduling
apscheduler>=3.10,<4

# Monitoring & error tracking
sentry-sdk[fastapi]==2.60.0

# Email
sendgrid==6.12.5

# File type detection (Python 3.13+ compatibility - replaces deprecated imghdr)
filetype==1.2.0

# Cloud storage (S3 / Cloudflare R2)
boto3==1.43.10

# Structured logging
python-json-logger==3.3.0

# AI/ML dependencies (Grapple AI Coach)
# TEMPORARILY DISABLED: Large CUDA dependencies cause deployment failures
# Re-enable when migrating AI features to separate microservice or using CPU-only torch
# groq==0.14.0
# together==1.5.35
# sentence-transformers==3.3.1
# tiktoken==0.8.0
