# Development environment variables for frontend
# Vite will use the proxy configuration in vite.config.ts
# to forward /api requests to http://localhost:8000

# Leave empty to use relative path with proxy
VITE_API_URL=
